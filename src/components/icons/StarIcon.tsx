/** Filled star — "default for new sessions" marker on a provider card.
    Icon-only, paired with a `title` tooltip by the caller. */
export function StarIcon() {
  return (
    <svg width="14" height="14" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path
        d="M8 2.2 9.7 6l4.1.4-3.1 2.8.9 4-3.6-2.1-3.6 2.1.9-4-3.1-2.8L6.3 6 8 2.2Z"
        fill="currentColor"
      />
    </svg>
  );
}
